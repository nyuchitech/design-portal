---
title: "Sovereignty — nyuchi design portal"
description: "Technology sovereignty assessment: licensing, governance, and self-hosting analysis for the Mukoko stack."
---

import { SEED_SOVEREIGNTY_SUMMARY, SEED_REMOVED_TECHNOLOGIES } from '@/lib/architecture'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Badge } from '@/components/ui/badge'

export const riskColors = {
  none: "bg-[var(--color-malachite)] text-black",
  low: "bg-[var(--color-gold)] text-black",
  removed: "bg-destructive text-destructive-foreground",
}

# technology sovereignty

A full assessment of every technology in the Mukoko stack, evaluated for licensing, governance risk, and self-hosting capability.

## Full Sovereignty Assessment

Every technology evaluated across licensing, governance, forkability, and self-hosting dimensions.

<div className="mt-8 overflow-x-auto rounded-xl border border-border">
  <table className="w-full text-sm">
    <thead>
      <tr className="border-b border-border bg-muted/50">
        <th className="px-4 py-3 text-left font-medium text-foreground">Technology</th>
        <th className="px-4 py-3 text-left font-medium text-foreground">Role</th>
        <th className="px-4 py-3 text-left font-medium text-foreground">License</th>
        <th className="px-4 py-3 text-left font-medium text-foreground">Governance</th>
        <th className="px-4 py-3 text-left font-medium text-foreground">Risk</th>
        <th className="px-4 py-3 text-left font-medium text-foreground">Forkable</th>
        <th className="px-4 py-3 text-left font-medium text-foreground">Self-Hostable</th>
      </tr>
    </thead>
    <tbody>
      {SEED_SOVEREIGNTY_SUMMARY.map((tech) => (
        <tr key={tech.technology} className="border-b border-border last:border-0">
          <td className="px-4 py-3 font-medium text-foreground">{tech.technology}</td>
          <td className="px-4 py-3 text-muted-foreground">{tech.role}</td>
          <td className="px-4 py-3 text-muted-foreground">{tech.license}</td>
          <td className="px-4 py-3 text-muted-foreground">{tech.governance}</td>
          <td className="px-4 py-3">
            <span className={`inline-flex items-center rounded-full px-2 py-0.5 text-xs font-medium ${riskColors[tech.sovereigntyRisk] ?? "bg-muted text-muted-foreground"}`}>
              {tech.sovereigntyRisk}
            </span>
          </td>
          <td className="px-4 py-3 text-muted-foreground">{tech.forkable ? "Yes" : "No"}</td>
          <td className="px-4 py-3 text-muted-foreground">{tech.selfHostable ? "Yes" : "No"}</td>
        </tr>
      ))}
    </tbody>
  </table>
</div>

### Risk Rating Legend

<div className="mt-4 flex flex-wrap gap-4">
  <div className="flex items-center gap-2">
    <span className="inline-block size-3 rounded-full bg-[var(--color-malachite)]" />
    <span className="text-sm text-muted-foreground">None — fully open, no governance risk</span>
  </div>
  <div className="flex items-center gap-2">
    <span className="inline-block size-3 rounded-full bg-[var(--color-gold)]" />
    <span className="text-sm text-muted-foreground">Low — minor concerns, mitigated</span>
  </div>
  <div className="flex items-center gap-2">
    <span className="inline-block size-3 rounded-full bg-destructive" />
    <span className="text-sm text-muted-foreground">Removed — replaced due to unacceptable risk</span>
  </div>
</div>

## Removed Technologies

Technologies that were evaluated and removed from the stack due to licensing, governance, or sovereignty concerns.

<div className="mt-8 grid gap-6">
  {SEED_REMOVED_TECHNOLOGIES.map((tech) => (
    <Card key={tech.name}>
      <CardHeader>
        <div className="flex items-start justify-between gap-2">
          <CardTitle className="font-serif text-lg">
            {tech.name}
          </CardTitle>
          <Badge variant="destructive" className="shrink-0 font-mono text-xs">
            removed
          </Badge>
        </div>
      </CardHeader>
      <CardContent className="space-y-3">
        <p className="text-sm text-muted-foreground">
          {tech.reason}
        </p>
        <div>
          <p className="text-xs font-medium uppercase tracking-wide text-foreground">
            Replacement
          </p>
          <p className="mt-1 text-sm text-muted-foreground">
            {tech.replacement}
          </p>
        </div>
        <div>
          <p className="text-xs font-medium uppercase tracking-wide text-foreground">
            Migration Path
          </p>
          <p className="mt-1 text-sm text-muted-foreground">
            {tech.migrationPath}
          </p>
        </div>
      </CardContent>
    </Card>
  ))}
</div>
